# === WHITELIST KETAT ===
# Default: abaikan SEMUA. Hanya file aman (terenkripsi + README) yang boleh masuk repo.
# Mencegah kebocoran: dashboard.html (sumber), dashboard_seed.html (DATA plaintext!),
# projects.json, history.json, *.xlsx, quote PDF, gaji, dan script build.
*

# Pengecualian — hanya ini yang di-publish:
!.gitignore
!.nojekyll
!index.html
!README.md

# Workflow keep-alive Supabase (ping berkala agar project free-tier tidak di-pause).
# Aman: hanya berisi anon key yang memang sudah publik di index.html.
!.github/
!.github/workflows/
!.github/workflows/supabase-keepalive.yml

# EV Hauling Simulator (di-iframe oleh app) — aman dipublish: tanpa data rate/gaji/project,
# self-contained (CDN only), hanya kredit penulis.
!ev-hauling-simulator-v2.1-swap-charge.html
!ev-hauling-simulator-v8_3.html
!ev-hauling-simulator-v9-loc.html
!ev-hauling-simulator-v10-swap-loc.html
